DATABASE_URL="postgresql://user:password@host/momentum?sslmode=require"
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
JWT_SECRET=""
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="30d"
COOKIE_DOMAIN=""

# Origins liberadas no CORS (web), separadas por vírgula. Vazio = libera todas (dev).
CORS_ORIGIN=""

# Chave de criptografia do accessToken do GitHub (AES-256-GCM).
# Gere com: openssl rand -hex 32  (64 chars hex = 32 bytes)
TOKEN_ENC_KEY=""
